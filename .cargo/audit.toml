[advisories]
# Temporary exceptions with documented review date:
# - RUSTSEC-2023-0071 (rsa via sqlx-mysql in sqlx macro internals): the app uses SQLite only.
#   Review by: 2026-06-30 (remove when upstream sqlx no longer pulls sqlx-mysql/rsa into this graph).
ignore = [
  "RUSTSEC-2023-0071",
]
